// experimental/nuke/nuke-ui.js
// Staged NUKE UI (inactive until imported)

(function () {
  console.log("[NUKE] Experimental NUKE UI loaded (inactive)");

  window.NukeUI = {
    renderNukePanel() {
      console.warn("[NUKE] renderNukePanel called but NUKE UI is inactive");
    }
  };
})();
