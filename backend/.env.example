REDIS_URL = redis://:your_redis_password@redis-host:6379

PLAYER_AUTH_SECRET = a-very-long-random-string

# KMS signing key for vouchers (production mode)
KMS_SIGNING_KEY_ID = arn:aws:kms:us-west-2:123456789012:key/abcd-efgh

AWS_REGION = us-west-2

AUDIT_S3_BUCKET = your-audit-bucket

COOLDOWNS_FAIL_SAFE = deny

LOOT_COOLDOWN_SECONDS = 300
 
VOUCHER_TTL_SECONDS = 3600

# Hugging Face API configuration for Overseer AI personality
HF_API_KEY = your-huggingface-api-key
HF_MODEL = mistralai/Mixtral-8x7B-Instruct-v0.1

# Admin authentication credentials (required for admin routes)
ADMIN_USERNAME = your-admin-username
ADMIN_PASSWORD = your-secure-admin-password
ADMIN_SESSION_TTL_SECONDS = 86400

# Server signing key for vouchers (development mode only)
# This should be a 64-byte Ed25519 secret key encoded in base58
# In production, use KMS instead (NODE_ENV=production)
# SERVER_SECRET_KEY = your-base58-encoded-64-byte-secret-key

# Redis is REQUIRED in production for data consistency and replay protection
# Set to "false" to disable this check (NOT RECOMMENDED)
# REQUIRE_REDIS_IN_PRODUCTION = true

# Strict replay protection requires Redis to be available and connected
# Set to "false" to disable strict replay protection (NOT RECOMMENDED)
# STRICT_REPLAY_PROTECTION = true
